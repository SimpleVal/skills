# Repo-local Skill Configuration Docs

Accepted. Engineering skills remain reusable across repositories by reading repo-local skill configuration docs instead of hard-coding each target repository's documentation paths into the skills themselves. This records coding standards and code verification doc references in the workspace, avoiding per-repo skill forks while still letting the implementer orchestrator and code review workflow follow project-specific human documentation.
